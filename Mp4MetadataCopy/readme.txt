This script performs a very specific task: it reads metadata from 264-encoded mp4s and writes to the equivalant hevc-encoded mp4.

Vids encoded with 264 have filenames that end in .264.mp4, where hevc encoded files are simply .mp4.

